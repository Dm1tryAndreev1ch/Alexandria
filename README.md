# Электронный дневник-расписание БГУИР

Веб-приложение для группы 561401 БГУИР, реализованное на Python/Flask с интеграцией Google Calendar и Telegram бота.

## Возможности

- 📅 Расписание занятий из Google Calendar (табличный и календарный вид)
- 📁 Загрузка файлов и фото с привязкой к занятиям
- 📋 Очередь ответов для предметов ОАиП и История
- ✅ Персональные To-Do списки (список и календарь)
- 🤖 Telegram бот для загрузки файлов и подтверждения пользователей (запускается автоматически)
- 👥 Система регистрации с подтверждением администратором

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Alexandria
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
pip install email-validator  # Дополнительно для валидации email
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Заполните переменные окружения в `.env`:
- `DATABASE_URL` - строка подключения PostgreSQL (или SQLite для разработки)
- `SECRET_KEY` - секретный ключ Flask
- `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` - для Google Calendar API
- `GOOGLE_CALENDAR_ID` - ID календаря группы (уже указан)
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `ADMIN_USERNAME` - имя пользователя для первого администратора (без символа @)

6. Инициализируйте базу данных:
```bash
flask db init
flask db migrate -m "Initial migration"
flask db upgrade
```

7. Запустите приложение:
```bash
python main.py
```

Приложение автоматически запустит Telegram бота в фоновом режиме вместе с веб-сервером.

## Тестирование

```bash
pytest
```

Все тесты должны пройти успешно (12 passed).

## Развёртывание на WSGI сервере

Приложение готово к развёртыванию на uWSGI или Gunicorn:

```bash
gunicorn -w 4 -b 0.0.0.0:8000 main:application
```

Telegram бот будет запущен автоматически при старте приложения.

## Структура проекта

- `app/` - основное приложение Flask
- `telegram_bot/` - Telegram бот (интегрирован в main.py)
- `tests/` - автоматические тесты
- `migrations/` - миграции базы данных
- `uploads/` - загруженные файлы
- `venv/` - виртуальное окружение Python

## Особенности

- Telegram бот запускается автоматически вместе с приложением в отдельном потоке
- Все файлы привязываются к занятиям из Google Calendar
- Администратор подтверждает новых пользователей через Telegram бота командой `/approve <username>`
- Пользователи могут загружать файлы через веб-интерфейс или Telegram бота

## Лицензия

Проект создан для группы 561401 БГУИР.
